# Multiple Inheritance

Multiple inheritance occurs when a class inherits from more than one base class.

```
BaseClass1   BaseClass2
      \       /
       DerivedClass
```

Example:
- `Printer` and `Scanner` are base classes.
- `MultiFunctionPrinter` is the derived class.

The derived class combines functionality from both bases.
