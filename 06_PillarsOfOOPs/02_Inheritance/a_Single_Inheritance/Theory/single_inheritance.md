# Single Inheritance

Single inheritance is when one derived class inherits from one base class.

```
BaseClass
   |
DerivedClass
```

Example:
- `Animal` is the base class.
- `Dog` is the derived class.

The derived class inherits members from the base class and can add its own members.
